# evidence-cmd: bash -c 'S=plugins/foundry/scripts/serena-daemon.sh; bash -n "$S" && echo SYNTAX_OK; test -x "$S" && echo EXECUTABLE; grep -q "streamable-http" "$S" && echo STREAMABLE_HTTP_PRESENT; grep -q "9121" "$S" && echo PORT_9121_PRESENT; grep -Eq "transport.*sse|sse.*transport" "$S" && echo SSE_PRESENT || echo NO_SSE; grep -q "nohup uvx serena-mcp" "$S" && echo NOHUP_LAUNCH_OK; grep -q ".serena-daemon.pid" "$S" && echo PID_FILE_OK; for c in start stop status restart install-service uninstall-service; do grep -Eq "^  $c[)]" "$S" && echo "DISPATCH:$c"; done'
# evidence-for: FR-002, FR-004, NFR-002, NFR-004

SYNTAX_OK
EXECUTABLE
STREAMABLE_HTTP_PRESENT
PORT_9121_PRESENT
NO_SSE
NOHUP_LAUNCH_OK
PID_FILE_OK
DISPATCH:start
DISPATCH:stop
DISPATCH:status
DISPATCH:restart
DISPATCH:install-service
DISPATCH:uninstall-service
